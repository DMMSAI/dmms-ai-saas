/**
 * Bot runs as a standalone process (bot.mjs).
 * This file is kept empty to avoid duplicate instances.
 */
export async function startTelegramBot() {
  // No-op — bot runs via bot.mjs process, not inside Next.js
}
